# akita-booking

Source for **book.akita.ac** — the Akita AC quote funnel. Survey CTAs point at the self-serve booker (akita-bookings) via `?type=survey` / `?type=virtual`.

Git-connected Cloudflare Pages project (production branch `main`, no build command, output `/`).
